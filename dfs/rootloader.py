# This is a bit of a cheater program, but everything
# done here can be done by simply loading one program into
# the shell at a time. In order to save time on that
# issue, this program loads several files into the node
# and then invokes base.

# It is intended that this file is invoked in its parent
# directory. That is - it will just pull everything
# but itself in and load it up.
# The conten

import os
